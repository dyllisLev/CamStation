# 2026-09-09 녹화 재생 배포 기록

## 첫 배포와 롤백

Windows 운영 NUC에는 검증한 Viewer MSI 2.0.30을 설치했다. 설치된 실행 파일,
app.asar, 관리 서비스 해시는 테스트 PC의 MSI 설치 결과와 일치하며 설정과 클라이언트
식별 정보를 보존했다. 배포 채널의 MSI 다운로드 SHA-256도 원본과 일치했다.

서버 첫 배포 후 FFmpeg 5.1.7의 RTSP 입력에서 영상 시간축 손상을 확인했다.
입력 탐색 중 보관한 PTS/DTS 없는 선두 패킷과 이후 epoch 패킷이 섞여 MP4 영상
표본의 시간이 축소됐으며, 완성 조각 인덱스도 게시되지 않았다. 기존 TS/파일 입력
검사로는 재현되지 않았던 RTSP 경계다. 2026-09-09 22:06 KST에 이전
`220a588aa658b03a73bea21ac25a6f111246708c` 이미지로 롤백했다.
DB를 과거 백업으로 덮어쓰지 않았고, 해당 구간 8개 파일과 DB 행을 보존했다.
롤백 후 healthy, 재시작 0, SQLite 검사 정상, 녹화 8개 생성·증가를 확인했다.

## 수정과 검증 범위

- RTSP 입력에 `+nobuffer`를 적용해 탐색 중 보관한 선두 패킷을 버리고 실제
  timestamp가 있는 키프레임부터 기록한다.
- 트랙별 시작점을 각각 0으로 만드는 MP4 동작은 영상·음성 시간차를 잃으므로,
  모든 트랙의 epoch decode timestamp와 packet 기반 PRFT를 함께 보존한다.
- 재생 인덱스와 HLS는 첫 조각의 가장 이른 트랙 decode 시각을 공통 원점으로
  사용한다. 영상 시작 전 오디오는 HLS 시간에는 포함하지만 영상 보관 범위로
  표시하지 않는다. 첫 조각 이후 탐색도 같은 원점을 사용한다.
- 배포에 사용한 FFmpeg 5.1.7에서 합성 RTSP(H.264·HEVC), 조각 게시·파일 전환과
  명시적 2초 영상·음성 시작 차이 검사를 통과했다. Chrome HLS 첫 영상·중간 탐색·
  다음 파일 전환에 미디어 오류가 없었다. 기존 실제 중계 스트림 2개를 12.416초
  확인해 8개 파일, 246개 영상 프레임을 오류 없이 디코드했다. 실제 첫 파일의
  오디오 선행 987/1440ms도 인덱스에 보존됐다. 장시간·부하 시험은 실시하지 않았다.

## 직접 파일 재생과 다운로드

새 파일의 `/play`, `/download`는 저장 파일을 수정하지 않고 응답의 시간 메타데이터만
공통 영상·음성 원점으로 정규화한다. 패킷 내용, PRFT의 절대 NTP, 파일 길이와 바이트
위치는 보존해 Range 탐색을 유지한다. 기존 상대시각 fMP4와 일반 MP4는 원래 바이트를
그대로 응답한다. 저장·백업 원본과 HLS 조각은 변경하지 않는다.

Chrome 직접 MP4 재생에서는 전체 길이를 실제보다 길게 표시하는 한계가 남는다.
최초 표본은 실제 카메라가 아니라 testsrc2 영상과 sine 음성의 합성 파일이며,
영상 시작을 의도적으로 2초 늦췄다. AAC 선행 패킷을 포함해 음성은 0~11.029초,
영상은 2.021~11.021초에 있고, Chrome은 길이를 13.043초로 표시했다.
첫 영상과 10초·파일 끝 근처 탐색, 영상·음성 시각 차이는 유지됐다. 제품 B 화면과
공통 HLS 재생은 11.029초로 정상 처리한다.

당초 "마지막 프레임을 약 2초 유지한다"고 보고했으나, 이는 재생 3초 후 상태만
확인한 잘못된 추론이었다. 2026-09-10에 종료 이벤트까지 직접 측정한 결과,
10.8초부터 재생해 0.281초 후 종료했고 마지막 프레임에서 종료까지는 0.129초였다.
2초 정지는 없으며 종료 순간 표시 시각만 13.043초로 이동한다.

운영 녹화도 읽기 전용으로 별도 확인했다. 2026-09-10 06:30 KST 무렵 시작한
최근 완성 녹화 8개의 음성 시작은 영상보다 0.023~0.447초 늦었다. 2026-09-09
23:00 KST 배포 직후 첫 녹화 8개에서는 음성이 0.067~3.823초 먼저 기록됐다.
이는 각 파일의 첫 영상·음성 패킷 시각 차이이며 실제 동작과 소리의 동기 검증은 아니다.
대표 최신 파일 11109와 배포 첫 파일 10983의 직접 재생 길이는 각각 0.447초와
3.823초 길게 표시됐지만, 실제 끝 0.4초 전부터 재생하면 각각 0.408초와 0.554초
후 종료했다. 두 파일 모두 표시 길이 차이만큼 마지막 화면을 유지하지 않았다.
추가 표본 10987은 25초 안에 메타데이터 로딩이 끝나지 않아 종료 동작을 판정하지 않았다.
운영 파일·DB·서버 설정은 변경하지 않았다.

## 손상 구간 복구의 한계

대상은 첫 배포 중 종료된 ready 녹화 ID 10950–10957이다. 원본을 별도 root 전용
백업에 보존하고, 영상 패킷 순서·내용과 원래 오디오 timestamp를 유지하는 후보를
만들었다. 영상 시간은 같은 카메라의 정상 파일과 메타데이터로 확인한 명목 FPS로
다시 구성한다. 손실된 원래 프레임별 수신 시각·간격이나 정확한 입 모양 동기는
복원했다고 주장하지 않는다. 새 PRFT를 꾸며 넣지 않으며 기존 파일 재생 경로를 쓴다.

복구는 파일별 원자적 교체와 DB `file_size` 갱신으로 제한한다. 녹화 시작·종료 시각,
상태, 백업 상태, 파일 권한·소유자·수정 시각은 유지한다. 따라서 일부 파일의 복구된
전체 미디어 길이가 기존 DB 종료 경계보다 약간 길 수 있다. 다운로드에는 전체
패킷을 보존하며, 보관 범위의 기존 종료 시각을 추정값으로 늘리지 않는다.

2026-09-09 22:38 KST에 8개 파일의 복구를 적용했다. 교체 후 모든 파일에서
영상·음성 패킷 수·순서·내용 해시가 원본과 같고, 오디오 PTS/DTS와 timebase도
같음을 확인했다. DB는 `file_size`만 변경됐고 원본 백업과 파일 속성을 보존했다.
파일별 DB 잠금 구간은 최대 0.692초였으며 데몬 재시작 없이 녹화기 8개와 작성 중
파일 8개의 증가를 확인했다.

롤백 버전에서 자연 종료된 8개 녹화도 ready 상태다. 대표 파일의 영상 길이
1389.369초, 오디오 길이 1389.494초로 시간축 축소가 없었다.

## 운영 조각 경계 보완

`d9c375c` 재배포에서 실제 AAC 패킷이 같은 DTS를 공유해 sample duration이 0인
조각을 확인했다. 패킷 크기 466바이트인 정상 음성을 빈 표본으로 거부해 한 녹화의
인덱스가 46개에서 멈췄으며, 녹화 파일 자체는 계속 증가했다. 해당 조각의 실제
AAC 15프레임·H.264 30프레임은 배포 FFmpeg 5.1.7에서 오류 없이 디코드됐다.

비어 있지 않은 AAC의 0 duration만 허용하도록 수정했다. 빈 payload, 영상의 0
sample duration, 데이터 범위와 영상 보관 구간 검사는 유지한다. 실제 메타데이터
회귀를 수정 전 실패·수정 후 통과로 확인했고 49개 조각의 연속 인덱스 및 해당 실제
조각의 Chrome HLS 재생도 통과했다.

HEVC 입력의 첫 연결 실패가 남긴 영상 패킷 없는 파일도 정상 녹화로 노출하지 않는다.
완성 영상 조각 없이 종료된 파일은 보존하되 DB에 `failed`로 원자적으로 기록해
기존 파일 재생 범위에 섞이지 않게 한다. 오디오만 있는 실제 형식의 회귀 검사로
파일 보존과 재생 범위 제외를 확인했다.

## 최종 배포 결과

2026-09-09 23:00 KST에 서버 `47a4ad33bc020e00269c3c2bac6a9880460169dd`를 배포했다.
[Forgejo 실행 45](https://git.loc.hmini.me/dyllislev/CamStation/actions/runs/14)가 성공했고
OpenShip `dep_n2lN7Yna-AgjIHTq`가 ready 상태다. 이미지 revision 일치, healthy,
재시작 0, 마운트 보존, SQLite quick_check 정상과 외래 키 위반 0을 확인했다.
새 녹화 8개 모두 파일과 조각 수가 증가하고 PRFT 절대시각이 타당했다. 기존 녹화
8개의 정상 종료·파일 크기 일치·ffprobe, 라이브 8대·녹화기 8대·NVENC 2개와
내부·외부 health도 확인했다. 최종 수정 후 Go 전체 검사와 daemon build가 통과했다.

23:08 KST에 기존 녹화 10976의 인덱스를 46개에서 352개로 보완했다. 기존 46개
조각은 모든 필드가 같고 원본 파일 118,501,474바이트와 파일 속성은 보존했다.
DB 행은 마지막 실제 PRFT에 맞춘 ts_end만 변경했다. 영상 없는 10981은 파일을
그대로 두고 status/error/updated_at만 정정했으며, 앞서 복구한 8개 행은 유지했다.
수정 전 SQLite online backup과 정정 계획·journal을 별도 보존했다.
보정 후 누락 시각의 resolve가 `found`를 반환하고, manifest에 352개 조각과
ENDLIST가 있는지 확인했다. 영상 없는 행의 시간축 제외와 녹화기 8대 정상도 확인했다.

운영 Viewer 2.0.30에서 B 화면 초기 자동재생 없음, 녹화 선택, 배포 중 연결 실패
이후 다시 시도, 실제 영상 재생과 22:49:11 KST 일시정지를 확인했다. 마지막에는
기존 배치의 라이브 8대 화면으로 복귀했다. NUC\dyllislev 세션 1은 Active,
Viewer 서비스는 Running이며 제어 작업·캡처 작업 잔여 0, 드라이버 TCP·방화벽
추가 0과 스크립트 해시 일치를 확인했다. 최신 Viewer 창 캡처는 23:05 KST이며
PNG SHA-256은 `74f966b32c15009941901a0f2d03c620f8321e52a7475b90ee6f9c6ef80c9294`다.
